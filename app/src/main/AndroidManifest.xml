<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thoughtswriter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="util.JournalAPI"
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_border_color_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_border_color_24"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThoughtsWriter">
        <activity android:name=".Thoughtlist"/>
        <activity android:name=".PostActivityContent" />
        <activity android:name=".CreateAccountActitvity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>